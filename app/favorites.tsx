// Route entry. The screen itself lives outside app/ so that route files
// never import one another — see components/screens/FavoritesScreen.tsx
export { default } from '../components/screens/FavoritesScreen';
