// Route entry. The screen itself lives outside app/ so that route files
// never import one another — see components/screens/JournalScreen.tsx
export { default } from '../components/screens/JournalScreen';
