// Route entry. The screen itself lives outside app/ so that route files
// never import one another — see components/screens/ProfileScreen.tsx
export { default } from '../components/screens/ProfileScreen';
