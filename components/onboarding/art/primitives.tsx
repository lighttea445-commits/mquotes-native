/**
 * Moved to `components/art/primitives.tsx` — the art idiom is no longer
 * onboarding-only. Re-exported here so existing onboarding imports keep
 * working.
 */
export { STROKE, Sparkle } from '../../art/primitives';
